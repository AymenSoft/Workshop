<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aymen.movies">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".modules.splashScreen.userInterface.view.SplashScreenActivity"
            android:theme="@style/Theme.Background.White.FitsSystemWidows"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".modules.networkIssue.userInterface.view.NoInternetConnexionPopupActivity" android:theme="@style/AppTheme.Popup"/>

        <activity android:name=".modules.moviesList.userInterface.view.MoviesListActivity"/>
        <activity android:name=".modules.movieDetails.userInterface.view.MovieDetailsActivity" android:theme="@style/Theme.Background.White.FitsSystemWidows"/>

    </application>

</manifest>